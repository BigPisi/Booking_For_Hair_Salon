# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/salon_booking
spring.datasource.username=postgres
spring.datasource.password=0278
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.servlet.context-path=/

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:8000,http://127.0.0.1:8000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# JWT Configuration
jwt.secret=salonBookingSecretKeyForJWTTokenGeneration2023
jwt.expiration=86400000

# Application Configuration
app.name=Hair Salon Booking System
